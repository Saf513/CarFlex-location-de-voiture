const burgerMenu=document.querySelector(".burgerMenu");
const section=document.querySelector("#section-burger");
const home=document
burgerMenu.addEventListener('click',()=>{
section.style.display = (section.style.display === 'flex') ? 'none' : 'flex';});